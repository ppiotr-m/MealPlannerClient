<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="piotr.michalkiewicz.mealplannerclient">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activities.settings.EditWeightActivity"></activity>
        <activity android:name=".view.activities.settings.EditHeightActivity" />
        <activity android:name=".view.activities.settings.EditDietActivity" />
        <activity android:name=".view.activities.settings.EditAvoidedIngredientsActivity" />
        <activity android:name=".view.activities.settings.EditAllergiesActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.activities.settings.EditLocationActivity"
            android:label="@string/title_activity_edit_location" />
        <activity android:name=".view.activities.settings.EditPasswordActivity" />
        <activity android:name=".view.activities.settings.SettingsActivity" />
        <activity android:name=".view.activities.authorization.RegistrationActivity" />
        <activity android:name=".view.activities.recipes.CookingStepsActivity" />
        <activity android:name=".view.activities.recipes.IngredientsActivity" />
        <activity android:name=".view.activities.personalization.CustomizationFinishActivity" />
        <activity android:name=".view.activities.personalization.CookingTimeCustomizationActivity" />
        <activity android:name=".view.activities.personalization.ServingsCustomizationActivity" />
        <activity android:name=".view.activities.personalization.MealsNumberCustomizationActivity" />
        <activity android:name=".view.activities.personalization.DislikedIngredientsCustomization" />
        <activity android:name=".view.activities.personalization.AllergyCustomizationActivity" />
        <activity android:name=".view.activities.personalization.DietCustomizationActivity" />
        <activity android:name=".view.activities.personalization.StartCustomizationActivity" />
        <activity
            android:name=".view.activities.recipes.RecipeActivity"
            android:label="@string/title_activity_recipe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.activities.menus.MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.activities.authorization.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>